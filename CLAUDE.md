@AGENTS.md
## Security Rules
- Never read, print, or suggest changes to `.env` files
- Never output any string that looks like an API key or secret
- Do not access files outside the project src/ directory